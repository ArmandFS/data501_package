% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corpus.R
\name{read_corpus_json}
\alias{read_corpus_json}
\title{Read a saved OpenAlex response into a corpus data frame}
\usage{
read_corpus_json(path, min_words = 40L, verbose = TRUE)
}
\arguments{
\item{path}{Path to a JSON file holding an OpenAlex `works` response.}

\item{min_words}{Minimum abstract length in words. Shorter records are
dropped.}

\item{verbose}{Report how many records were parsed and dropped.}
}
\value{
A data frame with columns `id`, `doi`, `title`, `abstract`,
  `publication_date` and `publication_year`.
}
\description{
Parses a `works` response that was saved to disk. This is the same parsing
path [fetch_corpus()] uses, so it can be exercised in tests without touching
the network.
}
\examples{
f <- system.file("extdata", "openalex_sample.json", package = "styloprofile")
read_corpus_json(f)

}
\seealso{
[fetch_corpus()]
}
