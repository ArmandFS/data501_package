% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mahalanobis.R
\name{mahalanobis_sq}
\alias{mahalanobis_sq}
\title{Squared Mahalanobis distance via a Cholesky factor}
\usage{
mahalanobis_sq(x, center, chol_cov)
}
\arguments{
\item{x}{Numeric matrix with one row per observation and one column per
feature, or a numeric vector treated as a single observation.}

\item{center}{Numeric vector of length `ncol(x)`, the reference mean.}

\item{chol_cov}{Upper-triangular `p x p` Cholesky factor of the covariance,
as returned by [chol()].}
}
\value{
Numeric vector of squared distances, one per row of `x`, named with
  the rownames of `x` when it has any.
}
\description{
Computes the squared Mahalanobis distance for each row of `x`. Takes the
Cholesky factor rather than the covariance so that a reference profile can
factorise once and reuse it for every call.
}
\examples{
set.seed(1)
m <- matrix(rnorm(200), ncol = 2)
R <- chol(cov(m))
d <- mahalanobis_sq(m, colMeans(m), R)

# same answer as base R
all.equal(d, mahalanobis(m, colMeans(m), cov(m)), check.attributes = FALSE)

}
